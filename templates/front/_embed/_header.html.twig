<nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
    <div class="container">
        <a class="navbar-brand" href="{{ path('front_home') }}">SyBlog</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar01" aria-controls="navbar01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbar01">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {% for menuPage in menuPages %}
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ path('front_page', {slug: menuPage.slug}) }}">
                            {{ menuPage.title }}
                        </a>
                    </li>
                {% endfor %}
            </ul>
            <ul class="navbar-nav">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ path('front_blog_tags') }}">
                            <i class="fas fa-hashtag fa-fw"></i>Tags
                        </a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">
                            <i class="fas fa-user fa-fw"></i> {{ app.user.userIdentifier }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li>
                                <a href="{{ path('front_profile') }}" class="dropdown-item">
                                    <i class="far fa-address-card fa-fw me-1"></i>
                                    Profile
                                </a>
                            </li>
                            {% if is_granted('ROLE_ADMIN') %}
                                <li>
                                    <a href="{{ path('admin_dashboard') }}" class="dropdown-item">
                                        <i class="fas fa-fw fa-tachometer-alt me-1"></i>
                                        Admin Panel
                                    </a>
                                </li>
                            {% endif %}
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a href="{{ path('front_logout') }}" class="dropdown-item">
                                    <i class="fas fa-sign-out-alt fa-fw me-1"></i>
                                    Logout
                                </a>
                            </li>
                        </ul>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a href="{{ path('front_login') }}" class="nav-link px-2">Login</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('front_register') }}" class="nav-link px-2">Sign up</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
