{% extends 'front/layouts/body.html.twig' %}

{% block title %}{{ page.title }}{% endblock %}

{% block content %}
    <div class="card mb-4">
        <div class="card-body">
            <h1 class="display-5">{{ page.title }}</h1>
            {{ page.content|raw }}
        </div>
    </div>
{% endblock %}
