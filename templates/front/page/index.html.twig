{% extends 'front/layouts/body.html.twig' %}

{% block title %}{{ page.title }}{% endblock %}

{% block content %}
    <div class="card">
        <div class="card-header">
            <h1>{{ page.title }}</h1>
        </div>
        <div class="card-body">
            {{ page.content|raw }}
        </div>
    </div>
{% endblock %}
