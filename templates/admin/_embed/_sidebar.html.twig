{% set _r = app.request.get('_route') %}

<nav class="sb-sidenav accordion sb-sidenav-dark" id="sidenavAccordion">
    <div class="sb-sidenav-menu">
        <div class="nav">
            <a class="nav-link{{ _r == 'admin_dashboard' ? ' active' : '' }}" href="{{ path('admin_dashboard') }}">
                <div class="sb-nav-link-icon"><i class="fas fa-fw fa-tachometer-alt"></i></div>
                Dashboard
            </a>
{#            <a class="nav-link{{ _r starts with 'admin_authenticatable' ? '' : ' collapsed' }}" href="#" data-bs-toggle="collapse" data-bs-target="#collapseLayouts"#}
{#               aria-expanded="{{ _r starts with 'admin_authenticatable' ? 'true' : 'false' }}" aria-controls="collapseLayouts">#}
{#                <div class="sb-nav-link-icon"><i class="fas fa-fw fa-users-cog"></i></div>#}
{#                Authenticatable#}
{#                <div class="sb-sidenav-collapse-arrow"><i class="fas fa-angle-down"></i></div>#}
{#            </a>#}
{#            <div class="collapse{{ _r starts with 'admin_authenticatable_admin' ? ' show' : '' }}" id="collapseLayouts" aria-labelledby="headingOne" data-bs-parent="#sidenavAccordion">#}
{#                <nav class="sb-sidenav-menu-nested nav">#}
{#                    <a class="nav-link" href="#">#}
{#                        User List#}
{#                    </a>#}
{#                    {% if is_granted('ROLE_SUPER_ADMIN') %}#}
{#                        <a class="nav-link{{ _r starts with 'admin_authenticatable_admin' ? ' active' : '' }}"#}
{#                           href="{{ path('admin_authenticatable_admin_index') }}">#}
{#                            Admin List#}
{#                        </a>#}
{#                    {% endif %}#}
{#                </nav>#}
{#            </div>#}
        </div>
    </div>
    <div class="sb-sidenav-footer">
        <div class="small">Logged in as:</div>
        {{ app.user.email }}
    </div>
</nav>
